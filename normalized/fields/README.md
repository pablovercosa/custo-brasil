# Campos XML

Este diretório conterá metadados estruturados de campos XML extraídos dos schemas XSD do corpus.
Cada entrada documenta: nome do campo, tipo, cardinalidade, padrão, enumeração e caminho XPath.
Populado durante a fase de análise de schemas.
